// const access_token = localStorage.getItem('access_token');
// export const stringify = (params = {}) => `?${Object.entries({ ...params, access_token }).map(([ key, value ]) => (value === null ? key : `${key}=${value}`)).join('&')}`;
